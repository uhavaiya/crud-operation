import React from 'react'

const About = () => {
  return (
    <div className="container">
      <div className="py-4">
        <h1> About Page</h1>
        <p className="lead">First is default natural sorting order, this is done by comparable interface in lang package, here the elements are sorting on asscending order(0–9 & A-z). Here you need not do do anything, everything is done by comareTo(x)
          Second is customizable sorting order, here compare(x,y) of comparator interface from util pacakge here you can customize your sorting order according to your requirement.
        </p>
        <p className="lead">First is default natural sorting order, this is done by comparable interface in lang package, here the elements are sorting on asscending order(0–9 & A-z). Here you need not do do anything, everything is done by comareTo(x)
          Second is customizable sorting order, here compare(x,y) of comparator interface from util pacakge here you can customize your sorting order according to your requirement.
        </p>
        <p className="lead">First is default natural sorting order, this is done by comparable interface in lang package, here the elements are sorting on asscending order(0–9 & A-z). Here you need not do do anything, everything is done by comareTo(x)
          Second is customizable sorting order, here compare(x,y) of comparator interface from util pacakge here you can customize your sorting order according to your requirement.
        </p>
        <p className="lead">First is default natural sorting order, this is done by comparable interface in lang package, here the elements are sorting on asscending order(0–9 & A-z). Here you need not do do anything, everything is done by comareTo(x)
          Second is customizable sorting order, here compare(x,y) of comparator interface from util pacakge here you can customize your sorting order according to your requirement.
        </p>
      </div>
    </div>
  );

};

export default About
